name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v4
        with:
          # The version check compares .version against HEAD^.
          fetch-depth: 2

      - uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-go@v5
        with:
          go-version-file: packages/code-storage-go/go.mod

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: ./.github/actions/check-package-versions

      - run: pnpm install
        working-directory: packages/code-storage-typescript

      - uses: moonrepo/setup-toolchain@v0
        with:
          auto-install: true

      - run: python scripts/check_legacy_routes.py

      - run: python -m unittest discover -s scripts/tests -v

      - run: moon :test
